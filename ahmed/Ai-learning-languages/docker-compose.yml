version: "3.9"

services:
  spanish-tutor:
    build: .
    ports:
      - "9562:5000"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      # Mount temp/ so audio files survive container restarts (optional)
      - ./temp:/app/temp
    restart: unless-stopped
